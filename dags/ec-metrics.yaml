ec-metrics:
  predictions: $var(result_folder)/test_prediction
  targets: $var(gt_folder)
  output_folder: $var(result_folder)
  heatmap_key: heatmap
  mask_key: mask
  nbins: $var(nbins, default=100)
  # pixel_auroc_max_fpr: $var(px_maxfpr, default=0.3)
  # image_auroc_max_fpr: $var(img_maxfpr, default=0.3)
  grabber:
    num_workers: $var(nproc, default=0)
    prefetch: $var(prefetch, default=2)
